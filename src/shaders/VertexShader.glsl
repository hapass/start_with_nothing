attribute vec2 vertex_position;
uniform vec2 screen_size;

void main() {
    //vec2 real_position = (vertex_position / screen_size) * 2.0 - 1.0;
    //gl_Position = vec4(real_position * vec2(1, -1), 0, 1);
}